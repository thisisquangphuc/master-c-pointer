#include <stdio.h>
#include <conio.h>
#include <stdlib.h>

void main()
{
	/*Declaration*/
	/*Pointer assignments*/
	/*Pointer vs Array*/
	/*Get size of Array*/
	/*Example 1: Write a program uses pointer to array in order to get data from user \
	and increase 10 for all elements, show the output */
	int a=10,b=5,c=9,d=10;
	int *p1 = (int *)malloc(sizeof(int));
	*p1=11;
	printf("Address of p: %x\n",&p1);
		
	getch();
}